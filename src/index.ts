import {
	type Schema,
	type KeyConfiguration,
	type Request,
	type Error,
} from "./interfaces";
import { validate, validateSchema } from "./validate";

export { type Schema, type KeyConfiguration, type Request, type Error };
export { validate, validateSchema };
